version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    healthcheck:
      test: "exit 0"
      interval: 30s
      timeout: 10s
      retries: 3