services:
  - type: web
    name: my-app
    env: docker
    plan: free
    buildCommand: 'docker build -t my-app .'
    startCommand: 'docker run -p 80:80 my-app'
    healthCheckPath: /
    autoDeploy: true